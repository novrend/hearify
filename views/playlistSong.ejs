<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <title>Add Song to Playlist</title>
</head>
<body style="background-color: #121212;">
    <div class="container p-5">
        <div class="d-flex">
            <img src="<%= playlist.thumbnailUrl %>" class="rounded me-4" width="200">
            <div>
                <h1 class="text-light"><%= playlist.name %></h1>
                <h3><a href="/playlists/<%= playlist.id %>/edit" class="btn btn-link">Edit playlist</a></h3>
            </div>
        </div>
        <form action="/playlists/<%= playlist.id %>/edit/song" class="text-light fw-lighter my-4" method="post">
            <h1>Add song to your playlist</h1>
            <table class="table table-borderless my-4 text-light fw-lighter">
                <thead class="table-bordered">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Title</th>
                        <th scope="col">Artist</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody class="table-group-divider">
                    <% let i = 1; for (obj of playlist.PlaylistSongs) { %>
                        <tr>
                            <td><%= i %></td>
                            <td><%= obj.Song.title %></td>
                            <td><%= obj.Song.artist %></td>
                            <td>
                                <a href="/playlists/<%= playlist.id %>/edit/song/<%= obj.Song.id %>" class="btn btn-outline-dark text-light fw-lighter">Add Song</a>
                            </td>
                        </tr>
                    <% i++; } %>
                </tbody>
            </table>
        </form>
    </div>
</body>
</html>