<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css">
    <title>Add New Song</title>
</head>
<body style="background-color: #121212;">
    <div class="container p-5 mt-5 rounded col-4" style="background-color: #242424; box-shadow: 0 4px 8px rgba(0,0,0,.05);">
        <a href="/songs" class="btn btn-outline-secondary btn-sm">←</a>
        <div class="mb-4 text-center text-light d-flex justify-content-center">
            <h1>Add New Song</h1>
        </div>
        <form action="/songs/add" method="post">
            <% err ? err = JSON.parse(err) : "" %>
            <% input ? input = JSON.parse(input) : "" %>
            <div class="form-floating mb-3 text-light">
                <input name="title" type="text" class="form-control bg-dark text-light fw-light" id="title" placeholder="Title" autocomplete="off" value="<%= input ? input.title : "" %>">
                <label for="title">Title</label>
                <div class="fw-light text-danger"><%= err ? err.title : "" %></div>
            </div>
            <div class="form-floating mb-3 text-light">
                <input name="artist" type="text" class="form-control bg-dark text-light fw-light" id="artist" placeholder="Artist" value="<%= input ? input.artist : "" %>" autocomplete="off">
                <label for="artist">Artist</label>
                <div class="fw-light text-danger"><%= err ? err.artist : "" %></div>
            </div>
            <div class="form-floating d-grid mb-3">
                <button type="submit" class="btn btn-outline-secondary btn-lg">Add new song</button>
            </div>
        </form>
    </div>
</body>
</html>