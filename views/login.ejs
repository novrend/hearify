<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css">
    <title>Login</title>
    <style>
        .password{
            float: right;
            margin-right: 15px;
            margin-top: -35px;
            position: relative;
            z-index: 2;
            cursor: pointer;
        }
    </style>
</head>
<body style="background-color: #f7f9fb;">
    <div class="container p-5 mt-5 rounded col-4" style="background-color: white; box-shadow: 0 4px 8px rgba(0,0,0,.05);">
        <div class="mb-4 text-center"><h1>Login</h1></div>
        <form action="/login" method="post">
            <div class="text-danger"><%= err ? err : "" %></div>
            <div class="form-floating mb-3">
                <input name="email" type="text" class="form-control" id="email" placeholder="name@example.com" autocomplete="off">
                <label for="email">Email address or username</label>
            </div>
            <div class="form-floating mb-3">
                <input name="password" type="password" class="form-control" id="password" placeholder="Password">
                <i class="fa password" id="pwd" aria-hidden="true"></i>
                <label for="password">Password</label>
                <div class="d-none" id="mark">!&#x20DD;</div>
                <p id="strength" class="d-none"></p>
            </div>
            <div class="form-floating d-grid mb-3">
                <button type="submit" class="btn btn-primary btn-lg">Login</button>
            </div>
            <div class="text-center">Don't have an account? <a href="/register">Register</a></div>
        </form>
    </div>
    <script>
        document.querySelector('#pwd').classList.toggle("fa-eye");
        document.querySelector('#pwd').addEventListener('click', (e) => {
            if (document.querySelector('#password').getAttribute("type") === 'text') {
                document.querySelector('#password').setAttribute("type", "password");
            } else {
                document.querySelector('#password').setAttribute("type", "text");
            }
            document.querySelector('#pwd').classList.toggle("fa-eye-slash");
        })
    </script>
</body>
</html>